version: '3'
services:
  nginx: 
    image: nginx:stable-alpine
    ports: 
      - "80:80"
    depends_on:
      - nest-app
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf
  nest-app:
    image: 'fonsecabarreto/nest-app:latest'
    restart: on-failure
    ports:
      - '81:3000'