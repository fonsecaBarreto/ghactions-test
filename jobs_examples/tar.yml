      - name: Run a multi-line script
        run: |
          mkdir ../release-files
          cp  docker-compose-prod.yml ../release-files
          tar -cvf deploy.tar ../release-files/
        



          ssh root@bluechickenwings.online << 'ENDSSH'
rm -rf app &&
mkdir app &&
tar xf test-node-app.tar.gz -C app &&
rm test-node-app.tar.gz 
ENDSSH